package com.groceryrun.app.enums;

/**
 * Enum for the roles of a user in the application
 */
public enum Role {
    USER, // User role for standard signed-in users
    ADMIN // Admin role for users who need to manage stores, aisles, and categories
}
